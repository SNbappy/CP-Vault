#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;

vector<int> adj[N];
bool vis[N];

void dfs(int vertex){
    
}

int main(){
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        int u, v;
        cin >> u >> v;

    }
    
}